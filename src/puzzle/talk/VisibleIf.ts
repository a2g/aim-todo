
export enum VisibleIf {
  // from aim-todo-enums.json, and must stay in sync with.
  Met = 'visibleIfMet',
  NotMet = 'visibleIfNotMet',
  Owned = 'visibleIfOwned',
  NotOwned = 'visibleIfNotOwned',
  Exists = 'visibleIfExists',
  NotExists = 'visibleIfNotExists',
}
