
export enum ChoiceType {
  // from todo-enums.json, and must stay in sync with.
  None,
  ViewableOnce = 'viewableOnce',
  ExecutableOnce = 'executableOnce',
  ExecutableOncePerConversation = 'executableOncePerConversation'
}
